# 五实验验证完整手册（从 Windows 主机零起步）

> 适用前提：代码均已正确，且你有一台装了 Windows 的宿主机，准备用 VirtualBox 起一台 Ubuntu 22.04 虚拟机做全部验证。

---

## 阶段 A：Windows 主机上的准备

### A1. 安装 VirtualBox
1. 浏览器打开 `https://www.virtualbox.org` → Downloads → 选 **Windows hosts** 安装包。
2. 双击下载的 `.exe`，一路 Next 安装（可取消勾选 "Oracle Cloud" 那类附加项）。
3. 装完打开 VirtualBox 管理器，确认能正常启动。

### A2. 下载 Ubuntu 22.04 ISO
1. 浏览器打开 `https://ubuntu.com/download/desktop` → 选 **Ubuntu 22.04.4 LTS**（或更新的 22.04.x）。
2. 下载得到 `ubuntu-22.04.4-desktop-amd64.iso`（约 4~5 GB），记住存放路径，比如 `D:\ISO\`。

### A3. 新建虚拟机
在 VirtualBox 管理器：
1. 点 **"新建"**（New）。
2. 名称填 `oscd-vm`；类型选 **Linux**；版本选 **Ubuntu (64-bit)**；下一步。
3. **内存**：拖到 **4096 MB**（4GB，内核编译最低要求；有 8GB 更好）。下一步。
4. **硬盘**：选 "现在创建虚拟硬盘" → 下一步。
5. 硬盘文件类型 **VDI** → 下一步。
6. 存储 **动态分配**（Dynamically allocated）→ 下一步。
7. 文件位置默认，大小设为 **40 GB**（内核源码+编译产物约 20GB，留余量）→ 完成。

### A4. 挂载 ISO 并启动安装
1. 选中 `oscd-vm` → 点 **"设置"**（Settings）→ **存储**（Storage）→ 控制器 "IDE" 下的 **空光驱** → 右侧光盘图标 → **选择/创建虚拟光盘** → 找到 `D:\ISO\ubuntu-22.04.4-desktop-amd64.iso` → 确定。
2. **设置 → 系统 → 处理器**：把 CPU 数量调到 **2~4**（内核编译用 `make -j$(nproc)`）。
3. **设置 → 显示 → 屏幕**：显存调到 **128 MB**（否则 Ubuntu 桌面卡）。
4. 点 **"启动"**（Start）→ 进入 Ubuntu 安装界面。
5. 安装选项：
   - 语言中文/英文均可 → "安装 Ubuntu"。
   - 键盘布局默认 → 继续。
   - "正常安装" → 勾 "安装时下载更新" 和 "安装第三方软件" → 继续。
   - "清除整个磁盘并安装 Ubuntu"（这是虚拟磁盘，放心）→ 继续 → 确认。
   - 时区选 **上海** → 继续。
   - 创建用户：姓名 `oscd`、计算机名 `oscd-vm`、用户名 `oscd`、密码自己设（记住它，sudo 要用）→ 继续。
6. 等安装完 → 点 "现在重启" → 弹出光驱提示时按回车 → 用 `oscd` 登录桌面。

> 到此你已"进入虚拟机"。下面在 VM 里做基础设置。

---

## 阶段 B：VM 首次启动后的基础设置

### B1. 安装增强功能（Guest Additions）—— 为了共享文件夹
1. 在 VM 窗口菜单栏：**设备（Devices）→ 安装增强功能（Insert Guest Additions CD image）**。
2. Ubuntu 会弹出光盘，打开终端执行：
```bash
sudo apt update
sudo apt install -y build-essential dkms linux-headers-$(uname -r)
sudo mount /dev/cdrom /mnt
sudo /mnt/VBoxLinuxAdditions.run
sudo reboot
```
重启后鼠标可自由进出 VM、分辨率自适应，共享文件夹可用。

### B2. 建立共享文件夹（把 Windows 的代码传进 VM）
**回到 Windows 主机：**
1. 在 D 盘建一个专门目录，比如 `D:\oscd-share`，把整个 `D:\oscd` 项目复制进去（或直接共享 `D:\oscd` 本身）。
2. VirtualBox 管理器：选中 `oscd-vm` → **设置 → 共享文件夹 → 添加（带 + 的文件夹图标）**：
   - 文件夹路径：`D:\oscd-share`
   - 文件夹名称：`oscd`
   - 勾 **自动挂载（Auto-mount）** 和 **固定分配（Make Permanent）**
   - 确定。

**回到 VM 终端：**
```bash
# 把当前用户加入 vboxsf 组，否则无权限访问共享文件夹
sudo usermod -aG vboxsf $USER
sudo reboot
```
重启后：
```bash
# 自动挂载点在 /media/sf_oscd
ls /media/sf_oscd        # 应看到 oscdsh/ oscdfs/ oscdk/ oscddrv/ ...
# 复制到工作目录（共享文件夹可直接用，但复制到 ~/ 编译更稳）
mkdir -p ~/oscd
cp -r /media/sf_oscd/* ~/oscd/
cd ~/oscd
```

> **备选：用 git 传代码**（不想配共享文件夹时）
> 在 Windows 把 `D:\oscd` 推到 GitHub，VM 里：
> ```bash
> sudo apt install -y git
> git clone <你的仓库URL> ~/oscd
> cd ~/oscd
> ```

---

## 阶段 C：安装编译依赖（VM 终端）

```bash
sudo apt update
sudo apt install -y build-essential libncurses-dev bison flex \
  libssl-dev libelf-dev dwarves fakeroot \
  linux-headers-$(uname -r) libreadline-dev libfuse-dev
```

---

## 阶段 D：按正确顺序验证五个实验

### 第 1 步｜实验一 + 实验五（oscdsh + monitor）
```bash
cd ~/oscd/oscdsh
make                      # 期望 0 error / 0 warning
./oscdsh
```
在 shell 内：
```
oscdsh> monitor
oscdsh> monitor process
oscdsh> monitor save out.txt
```
✅ 实验一、五完成。

### 第 2 步｜实验二（oscdfs）
```bash
cd ~/oscd/oscdfs
make
./oscdfs --init          # 生成 disk.img（只需一次）
./oscdfs                 # 交互：create / write / dir / cd / pwd
```
另开终端做 FUSE 挂载验证：
```bash
sudo mkdir -p /mnt/oscd && sudo chmod 777 /mnt/oscd
cd ~/oscd/oscdfs
sudo ./oscdfs -m /mnt/oscd -o allow_other -s &
ls /mnt/oscd
echo hi > /mnt/oscd/home/oscd/new.txt && cat /mnt/oscd/home/oscd/new.txt
fusermount -u /mnt/oscd
```
✅ 实验二完成。

### 第 3 步｜实验四（oscddrv）先在原始内核验证一次
```bash
cd ~/oscd/oscddrv
make
sudo insmod oscddrv.ko
sudo dmesg | tail -3          # 见 "module loaded successfully"
ls -l /dev/oscddrv            # 自动出现字符设备
sudo chmod 666 /dev/oscddrv
gcc test.c -o test && ./test
sudo rmmod oscddrv
```
✅ 实验四在原始内核验证通过（这个 .ko 匹配当前内核）。

### 第 4 步｜实验三（osck）自定义内核——唯一要重启的步骤
```bash
cd ~/oscd/oscdk
apt source linux-image-unsigned-$(uname -r)
# 得到 linux-hwe-6.8-6.8.0/ 目录
cd linux-hwe-6.8-6.8.0
patch -p1 < ../patches/0001-add-proc-syscalls.patch

cp /boot/config-$(uname -r) .config
make olddefconfig
sed -i 's/^CONFIG_BCACHEFS_FS=m$/# CONFIG_BCACHEFS_FS is not set/' .config
make olddefconfig
make -j$(nproc)               # 10~30 分钟，插电进行

sudo make modules_install
sudo make install
sudo reboot
```
重启时 **GRUB 菜单里用方向键选 `6.8.12-oscdk`**（若 GRUB 不显示，重启按住 Shift）。进入后：
```bash
uname -r                     # 必须显示 6.8.12-oscdk
cd ~/oscd/oscdk
gcc test.c -o test && ./test
```
✅ 实验三完成。

### 第 5 步｜关键：内核换了，实验四 .ko 必须重编
```bash
cd ~/oscd/oscddrv
make clean && make           # 针对新内核 6.8.12-oscdk 重新编译
sudo insmod oscddrv.ko
sudo chmod 666 /dev/oscddrv
gcc test.c -o test && ./test
```
✅ 实验四在新内核重新验证通过。

### 第 6 步｜五实验大一统总运行
```bash
# 终端1：总控 shell（实验一 + 实验五）
cd ~/oscd/oscdsh && ./oscdsh
#   oscdsh> monitor
#   提前在别处编译好测试程序：
#     gcc ~/oscd/oscdk/test.c -o /tmp/oscdktest
#     gcc ~/oscd/oscddrv/test.c -o /tmp/drvtest
#   oscdsh> !/tmp/oscdktest      # 实验三
#   oscdsh> !/tmp/drvtest        # 实验四

# 终端2：实验二 FUSE
cd ~/oscd/oscdfs && sudo ./oscdfs -m /mnt/oscd -o allow_other -s &
ls /mnt/oscd
```
✅ 五个实验同时存活，大一统验证完成。

---

## 顺序记忆口诀

**Windows 装 VirtualBox→下 ISO→建 VM（4G 内存/40G 盘/4 核）→装 Ubuntu→装增强功能→共享文件夹传代码→装依赖 → 实验一五 → 实验二 → 实验四（原内核）→ 实验三重编内核重启 → 实验四 .ko 重编 → 五者同跑。**

唯一易错点：**第 4 步重启换内核后，第 3 步编的 .ko 作废，第 5 步必须重新 `make` 一次。**

---

## 当前融合的边界（如实说明）

- ✅ **已真正融合**：实验五 `monitor` 是 oscdsh 的内置命令。
- ❌ **尚未实现**：融合计划里写的 oscdsh `task` 命令（直接调 548/549/550 系统调用）和 `oscddrv` 命令（直接 ioctl 控制驱动）目前**没有代码**。现在的"统一"是通过 oscdsh 执行外部程序（`!gcc` / `!/tmp/xxx`）来间接调度的。

如需把 `oscddrv` / `task` 内置命令补进 oscdsh，可后续开发。

---

## 常见故障速查

| 现象 | 原因 | 解决 |
|:--|:--|:--|
| `insmod: Invalid module format` | .ko 与运行内核版本不符 | 重 `make`（确保 `linux-headers-$(uname -r)` 对应新内核） |
| `/dev/oscddrv` 不存在 | 模块没加载或加载失败 | `dmesg` 看错误；检查 `class_create`/`device_create` |
| FUSE 挂载 `Permission denied` | 非 root 访问 | 加 `-o allow_other` 且 `sudo` 挂载 |
| `uname -r` 仍是原内核 | GRUB 没选新内核 | 重启进 GRUB 手动选 `6.8.12-oscdk` |
| oscdfs `failed to open` | 没 `--init` | 先 `./oscdfs --init` |
| 共享文件夹无权限 | 用户不在 vboxsf 组 | `sudo usermod -aG vboxsf $USER` 后重启 |
| 内核编译报 bcachefs 错 | 该模块与当前配置冲突 | `sed` 禁用 `CONFIG_BCACHEFS_FS` 后重编 |
